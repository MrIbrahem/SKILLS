read and anlyze:

-   `mwclient` real code in `mwclient_skills/code`
-   DeepWiki docs for `mwclient` in `mwclient_skills/deepWiki`

Then Using `.claude/skills/skill-writers/SKILL.md`, write mwclient claude code skill at `.claude/skills/mwclient`:

-   `references`
-   `skills` # for sub skills
-   `SKILL.md`
